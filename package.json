{
  "name": "edupath",
  "version": "1.0.0",
  "description": "EduPath AI Platform",
  "private": true,
  "scripts": {
    "start": "cd backend && npm start",
    "build": "cd backend && npm run build",
    "dev": "cd backend && npm run dev"
  },
  "engines": {
    "node": "20"
  }
}
